const select = "SELECT * FROM programme";
// const insert = `INSERT INTO yearsemester(yearsem_id, yearsem_year, yearsem_sem, yearsem_type) VALUES (${req.body.id},${req.body.year},${req.body.sem},"${req.body.semType}")`;

module.exports = {
    select
}